name: Deploy to GitHub Pages

on:
  push:
    branches: [master]

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - run: npm install

      - run: npm run release

      - name: Stamp service worker cache version
        run: sed -i "s/__CACHE_VERSION__/$(git rev-parse --short HEAD)/g" resources/public/sw.js

      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./resources/public
          cname: ff.dast.xyz
          exclude_assets: '.DS_Store,**/.DS_Store,.htaccess'
